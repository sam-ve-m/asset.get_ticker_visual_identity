apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-ticker-visual-identity-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/get_ticker_visual_identity
    rewrite:
      uri: /get_ticker_visual_identity
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80