apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-ticker-visual-identity-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/get-ticker-visual-identity
    rewrite:
      uri: /get-ticker-visual-identity
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80